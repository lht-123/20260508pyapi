version: '3.8'
services:
  web:
    build: .
    image: my-flask-app:latest
    container_name: flask_service
    ports:
      - "8080:8080"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
